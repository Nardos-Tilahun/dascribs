meta {
  name: Logout
  type: http
  seq: 4
}

post {
  url: {{baseURL}}/auth/logout
  body: none
  auth: none
}

headers {
  X-Session-Token: {{sessionId}}
}

settings {
  encodeUrl: true
  timeout: 0
}

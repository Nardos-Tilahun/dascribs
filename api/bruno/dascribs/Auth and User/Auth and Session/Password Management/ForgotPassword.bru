meta {
  name: ForgotPassword
  type: http
  seq: 1
}

post {
  url: {{baseURL}}/auth/forgot-password
  body: json
  auth: inherit
}

body:json {
  {
    "email": "user@example.com"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
